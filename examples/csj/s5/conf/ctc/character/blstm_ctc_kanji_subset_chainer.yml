parent: ./conf/ctc/word/blstm_ctc_word5_subset.yml
param:
  # framework
  backend: chainer

  # corpus
  data_size: subset
  label_type: kanji_wb

  # features
  num_stack: 3
  num_skip: 3

  # topology
  encoder_num_layers: 3
  subsample_list:
    - False
    - False
    - False
  fc_list: [256]
  # TODO: concat or subsampling (small mini-batch)

  # initialization
  char_init: False
